# Gitleaks configuration — extend with allowlists if CI reports false positives.
# See https://github.com/gitleaks/gitleaks#configuration

title = "CloudOpt gitleaks config"

[allowlist]
  description = "Known safe patterns"
  paths = [
    '''\.env\.example$''',
    '''package-lock\.json$''',
    '''\.cursor/plans/''',
  ]
