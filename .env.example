# CloudOpt environment configuration
# Copy to .env and adjust for local development.

# API
CLOUDOPT_API_HOST=0.0.0.0
CLOUDOPT_API_PORT=8000
CLOUDOPT_DEBUG=false

# Database (sync URL for Alembic; async for app)
CLOUDOPT_DATABASE_URL=postgresql+asyncpg://cloudopt:cloudopt@localhost:5432/cloudopt
CLOUDOPT_DATABASE_URL_SYNC=postgresql://cloudopt:cloudopt@localhost:5432/cloudopt

# Redis
CLOUDOPT_REDIS_URL=redis://localhost:6379/0

# CLI: API base URL for `cloudopt audit` commands
# CLOUDOPT_API_BASE_URL=http://127.0.0.1:8000

# Audit collectors (worker): max items per source per scan
# CLOUDOPT_AUDIT_SECURITY_HUB_MAX_FINDINGS=500
# CLOUDOPT_AUDIT_CONFIG_MAX_RULES=200

# AWS (optional for local dev)
# CLOUDOPT_AWS_REGION=us-east-1
# CLOUDOPT_AWS_ACCESS_KEY_ID=
# CLOUDOPT_AWS_SECRET_ACCESS_KEY=

# AI — OpenAI-compatible API (vLLM self-hosted or OpenAI cloud)
# CLOUDOPT_LLM_BASE_URL=http://localhost:8001/v1
# CLOUDOPT_LLM_API_KEY=EMPTY
# CLOUDOPT_LLM_CHAT_MODEL=Qwen/Qwen2.5-7B-Instruct
# CLOUDOPT_LLM_EMBED_MODEL=BAAI/bge-m3
# CLOUDOPT_EMBEDDING_DIMENSIONS=1024
# CLOUDOPT_OPENAI_API_KEY=
# CLOUDOPT_ANTHROPIC_API_KEY=

# Agent (tool-calling loop; set false if the backend model does not support tools)
# CLOUDOPT_AGENT_TOOLS_ENABLED=true
# CLOUDOPT_AGENT_MAX_TOOL_ROUNDS=6
